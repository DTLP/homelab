apiVersion: apps/v1
kind: Deployment
metadata:
  name: kyverno-reports-controller
  namespace: kyverno
spec:
  template:
    spec:
      containers:
        - name: controller
          resources:
            limits:
              cpu: "200m"
              memory: 1Gi
            requests:
              cpu: 50m
              memory: 128Mi
