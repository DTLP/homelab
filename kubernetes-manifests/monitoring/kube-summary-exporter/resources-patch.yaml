apiVersion: apps/v1
kind: Deployment
metadata:
  name: kube-summary-exporter
spec:
  template:
    spec:
      containers:
        - name: kube-summary-exporter
          resources:
            requests:
              cpu: 10m
              memory: 64Mi
            limits:
              cpu: 100m
              memory: 128Mi